<!DOCTYPE html>
<html>
  <head>
    <title>Yelpdemo</title>
    <%= render 'layouts/bootstrap' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
    <%= render 'layouts/navbar' %>
    <div class="container-fluid">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p> <!--enable us to send alerts to users (from devise)-->
    <%= yield %>
    <%= render 'layouts/footer' %>
  </div>
  </body>
</html>
